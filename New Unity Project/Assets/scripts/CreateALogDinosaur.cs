using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreateALogDinosaur : MonoBehaviour
{
    public GameObject thePrefab;

    void Start()
    {
        /*
        GameObject instance = new GameObject();
        thePrefab = Instantiate(thePrefab,
                               transform.position,
                               transform.rotation) as GameObject;

        thePrefab.transform.localScale = new Vector3 [0.25f, 0.25f, 0, 25f];

        thePrefab = Instantiate(thePrefab, thePrefab.transform.position, Quaternion.identity) as GameObject;
        thePrefab.transform.localScale = new Vector3(0.25f, 0.25f, 0.25f);
         */
    }
}
